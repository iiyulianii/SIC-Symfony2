{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Uoptica</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Mar</th>
                <td>{{ entity.mar }}</td>
            </tr>
            <tr>
                <th>Nse</th>
                <td>{{ entity.nse }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('uoptica') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('uoptica_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
