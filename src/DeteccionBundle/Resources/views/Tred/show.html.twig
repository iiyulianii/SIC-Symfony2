{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tred</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Mar</th>
                <td>{{ entity.mar }}</td>
            </tr>
            <tr>
                <th>Mac</th>
                <td>{{ entity.mac }}</td>
            </tr>
            <tr>
                <th>Nco</th>
                <td>{{ entity.nco }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tred') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('tred_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
