{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tred list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Mar</th>
                <th>Mac</th>
                <th>Nco</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tred_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.mar }}</td>
                <td>{{ entity.mac }}</td>
                <td>{{ entity.nco }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tred_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tred_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tred_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
