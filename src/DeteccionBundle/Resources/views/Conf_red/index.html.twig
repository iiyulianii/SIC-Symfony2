{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Conf_red list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Ip</th>
                <th>Ms</th>
                <th>Pe</th>
                <th>Dns1</th>
                <th>Dns2</th>
                <th>Mac</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('conf_red_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.ip }}</td>
                <td>{{ entity.ms }}</td>
                <td>{{ entity.pe }}</td>
                <td>{{ entity.dns1 }}</td>
                <td>{{ entity.dns2 }}</td>
                <td>{{ entity.mac }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('conf_red_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('conf_red_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('conf_red_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
